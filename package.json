{
  "name": "yfapp",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "cd backend && gunicorn app:app",
    "build": "cd backend && python -m pip install -r requirements.txt --no-cache-dir",
    "test-backend": "cd backend && python -c 'import flask; print(flask.__version__)'",
    "clean": "cd backend && rm -rf __pycache__ && pip cache purge"
  }
}

