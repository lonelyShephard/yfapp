{
  "name": "yfapp",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "start": "cd backend && python app.py",
    "build": "cd frontend && yarn install && yarn build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}

